# Data Model: Accessible Sign-up Flow

No new entities or schema changes.

The feature uses the existing entities:

- **Account**: created by Supabase Auth through the existing browser signup call.
- **Profile**: created by the existing database trigger for a new account.

No fields, relationships, policies, or persistence behavior change.
